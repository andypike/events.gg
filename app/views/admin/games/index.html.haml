- content_for :title do 
  Games

#games
  .row
    .large-12.columns

      %h1= pluralize(@games.size, "Game")

      = link_to "Add a new game", new_admin_game_path, class: "button"

      %table
        %thead
          %tr
            %th Name
            %th Status
            %td
        %tbody
          - @games.each do |game|
            %tr
              %td= game.name
              %td= game.status.titleize
              %td= link_to "Edit", edit_admin_game_path(game)
