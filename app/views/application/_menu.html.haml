%nav.top-bar
  %ul.title-area
    %li.name
      %h1= link_to "Esports Events", root_path 

  %section.top-bar-section
    %ul.left
      %li.divider
      %li.active= link_to "Feed", root_path

      - if current_user
        %li.divider
        %li= link_to "Logout", logout_path
      - else
        %li.divider
        %li= link_to "Login", login_path
        %li.divider
        %li= link_to "Sign Up", signup_path

      %li.divider
      %li= link_to "About", "#"

    - if current_user
      %ul.right
        %li
        %li= link_to current_user.name, settings_path